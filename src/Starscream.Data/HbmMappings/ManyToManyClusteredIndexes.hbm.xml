<hibernate-mapping xmlns="urn:nhibernate-mapping-2.2"
namespace="NovaEnergy.Data.HbmMappings"
assembly="NovaEnergy.Data.HbmMappings">
  <!--<database-object>
    <create>create clustered index ix on CompaniesToLocations(Company_id, Location_id)</create>
    <drop>drop index CompaniesToLocations.ix</drop>
  </database-object>
  <database-object>
    <create>create clustered index ix on ExecutorToCompany(Executor_id, Company_id)</create>
    <drop>drop index ExecutorToCompany.ix</drop>
  </database-object>
  <database-object>
    <create>create clustered index ix on MarketToCompany(Market_id, Company_id)</create>
    <drop>drop index MarketToCompany.ix</drop>
  </database-object>-->
</hibernate-mapping>